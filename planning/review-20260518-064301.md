# Review: Changes Since Last Commit

## Findings

1. **Low - README points readers to stale module-level test details**  
   `README.md:25` adds a headline claim that the market-data subsystem has 73 tests at 84% coverage and directs readers to `planning/MARKET_DATA_SUMMARY.md` for module-level details. The headline count matches the current static test-function count across `backend/tests/market`, but the cited details are internally stale: `planning/MARKET_DATA_SUMMARY.md:53` lists `test_simulator.py` as 17 tests, while the current file contains 19 test functions, and the table sums to 71 rather than 73. The same repo also has a newer review note in `planning/MARKET_DATA_REVIEW.md` reporting 73 passing tests at 91% coverage, so the new README sentence risks publishing stale coverage information. Update `planning/MARKET_DATA_SUMMARY.md` and confirm the current coverage number before using it as the README's source of truth.

## Notes

- Reviewed tracked changes in `.claude/settings.json` and `README.md`.
- The `.claude/settings.json` change is valid JSON. Current Claude Code hook docs describe exec-form command hooks using `command` plus `args`, so the `bash -c ...` shape is not itself a schema issue.
- `git diff --check HEAD` passed.
- I could not rerun the backend test suite in this sandbox: `uv --offline --extra dev` could not populate missing cached wheels, the checked-in `backend/.venv` does not contain `pytest`, and the system pytest environment is missing the `massive` dependency.
