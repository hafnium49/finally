# Code Review - 2026-05-18 06:20:44

Reviewed changes since `HEAD`.

## Findings

### Low - `README.md:17` says SSE/FastAPI wiring is not built, but the FastAPI router already exists

The new status row says `SSE ↔ FastAPI wiring | Not built`, while `backend/app/market/stream.py:17` defines an `APIRouter(prefix="/api/stream")` and `backend/app/market/stream.py:20` exposes `create_stream_router(price_cache) -> APIRouter`. What is not built is the root FastAPI app / HTTP server that mounts this router. As written, the README can mislead the next implementer into rebuilding the SSE router instead of wiring the existing router into the future app. Consider renaming the row to something like `Mount SSE router in FastAPI app | Not built`, or fold it into the existing `FastAPI HTTP layer` row.

### Low - `README.md:25` points to module-level test details that are internally inconsistent

The new README sentence directs readers to `planning/MARKET_DATA_SUMMARY.md` for module-level details. That summary says there are 73 tests overall, but its per-module table lists `test_simulator.py | 17` at `planning/MARKET_DATA_SUMMARY.md:53`; the current file has 19 test functions, and the table as written sums to 71. The README's headline count is consistent with a static count of test functions, but the referenced details contradict it. Update the summary table before relying on it as the README's source of detail.

## Verification

- Inspected `git diff HEAD -- README.md`.
- Checked `backend/app/market/stream.py` for existing FastAPI SSE router wiring.
- Counted test functions with `rg`, which found 73 `test_` functions under `backend/tests/market`.
- Attempted `uv run --extra dev pytest --cov=app --cov-report=term-missing` from `backend/`; initial run failed because uv tried to use a read-only cache path, and the retry with `UV_CACHE_DIR=/tmp/uv-cache` failed because network/DNS access is unavailable for a missing wheel.
- Attempted to use the existing `backend/.venv`, but it contains no `pytest` executable or installed dependencies.
