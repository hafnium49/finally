# FinAlly Project - the Finance Ally

All project documentation is in the `planning` directory.

The full specification is `planning/PLAN.md` (included in full below). The build is **complete** — `planning/SHIPPED.md` documents what was built, the agent-team orchestration that built it, and the bug-fix iteration log. The market-data subsystem was completed first and has its own design notes in `planning/MARKET_DATA_SUMMARY.md` and `planning/archive/`.

Consult these docs only when required. Treat `PLAN.md` as the source of truth for *what* the app does; treat `SHIPPED.md` as the source of truth for *what's actually in the repo*.

@planning/PLAN.md
